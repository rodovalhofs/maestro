# Changelog

## Unreleased — planned 0.3.0

GitHub delivery only. npm publication is deferred; no 0.3.0 registry release has been made.

- Bundle Maestro Prompt Designer with both npm adapters; install, upgrade, diagnose
  and remove both skills while preserving unrelated installations and legacy ownership.
- Integrate grilling, persistent approved local specifications and same-conversation
  handoff. Preserve complete project objectives across validated phases.
- Require candidate instruction review against objective, phase, compatibility and
  restrictions. Replace uncalibrated confidence/automatic loading with retrieval
  evidence, ambiguity reporting and explicit review decisions (JSON migration required).
- Keep short technical terms, normalize accents and avoid score inflation from
  repeated query terms. Greetings followed by tasks no longer bypass routing.
- Forward project context through batch routing; expose `--local-only` for both commands.
- Research confirmed gaps using minimal public technical queries; keep remote source
  review and installation authorization separate. No network in CLI search/route.
- Add curated bilingual retrieval evaluation and privacy, packaging and bundle lifecycle
  regression coverage. Evaluation measures a fixed set, not general selection accuracy.
- Publish only reviewed files using an exact allowlist. Inspect actual archives for
  unexpected/private files, symlinks, common secrets and personal paths; verify integrity.
